# ============================================================
# QuantumFlow GPU 加速依赖（仅 Linux）
# ============================================================
# Windows / macOS 不支持；GPU 加速必须在 Linux + NVIDIA 驱动下运行。
# vLLM 已自带 torch 与 nvidia-ml-py3，此处仅补 CUDA 相关可选组件。
#
# 安装：
#   pip install -r requirements.txt -r requirements-gpu.txt
#
# 注意：
#   - PyTorch 与系统 CUDA 版本必须严格匹配（12.4 或 13.0）
#   - FlashAttention 编译耗时较长，首次安装建议预留 30+ 分钟
#   - 若 FlashAttention 编译失败，可改用 xformers 作为替代
# ============================================================


# PyTorch（CUDA 12.4 版本，按需切换 cu130）
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.4.0


# NVIDIA 系统管理（GPUMonitor / 显存查询）
nvidia-ml-py3>=7.352.0


# FlashAttention 2（高性能注意力；需与 CUDA 严格匹配）
# 编译失败时改用 xformers：
#   model = AutoModelForCausalLM.from_pretrained(..., attn_implementation="xformers")
flash-attn>=2.5.0


# TensorRT-LLM（TensorRT 后端，可选）
# 体积大、编译时间长；不需要可注释掉
# nvidia-tensorrt>=10.0.0
