# ============================================================
# QuantumFlow 依赖清单
# ============================================================
# Python:  >= 3.10
# CUDA:    12.4 或 13.0（必须与 PyTorch 严格匹配）
# 平台:    Linux (推荐) / Windows 11 (WSL2 推荐) / macOS (仅 CPU 推理)
#
# 快速安装：
#   # 仅运行时
#   pip install -r requirements.txt
#
#   # 运行时 + GPU 加速（Linux）
#   pip install -r requirements.txt -r requirements-gpu.txt
#
#   # 运行时 + 开发工具（测试/格式化/类型检查）
#   pip install -r requirements.txt -r requirements-dev.txt
#
#   # 全量（开发 + GPU）
#   pip install -r requirements.txt -r requirements-gpu.txt -r requirements-dev.txt
# ============================================================


# ============================================================
# 1. Web 框架
# ============================================================
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
starlette>=0.36.0


# ============================================================
# 2. 数据验证与配置
# ============================================================
pydantic>=2.6.0
pydantic-settings>=2.2.0
pyyaml>=6.0.0
python-dotenv>=1.0.0


# ============================================================
# 3. 异步与 HTTP
# ============================================================
httpx>=0.27.0
aiofiles>=23.2.0


# ============================================================
# 4. Redis（队列、租户、集群状态）
# ============================================================
redis>=5.0.0


# ============================================================
# 5. gRPC + Protocol Buffers
# ============================================================
grpcio>=1.62.0
grpcio-tools>=1.62.0
grpcio-reflection>=1.62.0
protobuf>=4.25.0


# ============================================================
# 6. 监控与可观测性
# ============================================================
prometheus-client>=0.19.0
prometheus-fastapi-instrumentator>=6.1.0


# ============================================================
# 7. 日志
# ============================================================
structlog>=24.1.0
python-json-logger>=2.0.0


# ============================================================
# 8. 推理引擎与模型加载
# ============================================================
# HuggingFace Transformers（HF / TGI / SGLang 后端共用，跨平台）
transformers>=4.40.0
tokenizers>=0.15.0
huggingface-hub>=0.23.0
safetensors>=0.4.0

# vLLM：PagedAttention + Continuous Batching
# ⚠️ 官方不支持 Windows（pip install 在 Windows 上必失败）
# Windows / macOS 用户跳过 vLLM，使用 HuggingFace 后端（已包含在上方）
# Linux 用户在 requirements-gpu.txt 中按需安装
# vllm>=0.21.0; sys_platform == 'linux'

# 注意力加速（HF 后端可选，vLLM 不需要）
# xformers>=0.0.23  # FlashAttention 不可用时的替代


# ============================================================
# 9. CLI 与终端
# ============================================================
click>=8.1.0
rich>=13.7.0


# ============================================================
# 10. 工具库
# ============================================================
tenacity>=8.2.0        # 重试
orjson>=3.9.0          # 快速 JSON
psutil>=5.9.0          # 进程/系统信息
